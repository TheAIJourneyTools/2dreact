# 2dreact

React + TypeScript project scaffolded with Vite.

Commands:

- Install: `npm install`
- Dev: `npm run dev`
- Build: `npm run build`
- Preview: `npm run preview`

Files:

- `index.html` — app entry
- `src/main.tsx`, `src/App.tsx` — starter app
- `vite.config.ts`, `tsconfig.json` — config
